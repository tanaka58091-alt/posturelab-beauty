# POSTURE_AI_CHANGELOG.md — 変更履歴

最高峰化プロジェクトの実変更を記録する。各エントリに「変更前 → 変更後」の実測値を残す。

---

## 2026-08-10 — キープ用タイマー（562種中288種が時間指定だった）

**問題（実測）**
- 全562種の `duration` を集計したところ、**288種（51%）が「◯秒」「◯分」指定**
  （各30秒42件／1分37件／2分29件／30秒×3セット28件 …）
- これまで、頭の中で数えるか時計を見るしかなかった。
  40〜70代の初心者が姿勢を保ちながら正確に数えるのは難しく、途中で切り上げる原因になる

**対応**（`app.js` / `style.css`）
- 「やり方」の直後に **⏱ ◯秒をはかる（左右2回）** ボタンを追加（時間指定の種目にのみ表示）
- `parseHold()` が `duration` を解釈。実データ40パターンすべてで検証済み
  - 「各30秒」→ 30秒×2（右／左のラベル付き）
  - 「30秒 × 左右2セット」→ 30秒×4
  - 「1分」→ 60秒×1 ／「10秒 × 10回」→ 10秒×10
  - 「各指10秒」「各方向10回」は左右ではないので右／左のラベルを出さない
- 画面は大きな数字（66px）＋進捗リング。左右のあいだに5秒の持ちかえ時間を挟む
- 音は鳴らさず（自動再生がブロックされるため）、切り替わりと完了を**振動**で伝える

**タイマーが止まる問題の回避**（`app.js`）
- 当初 `setInterval` で1秒ずつ引く実装にしたところ、**画面を離れると間引かれて止まって見えた**
  （実測: 休憩フェーズで残り2のまま停止）
- 開始時刻からの経過秒でフェーズと残り時間を計算する方式に変更。
  40秒ぶんスキップしても「2回目・残25秒」と正しい位置に復帰する
- 画面に戻ったタイミング（`visibilitychange`）で即座に描き直す

### 変更前 → 変更後

| 指標 | 変更前 | 変更後 |
|---|---|---|
| 時間指定の種目（288種）の計時 | 自分で数える／時計を見る | **ボタン1つで大きなカウントダウン** |
| 左右がある種目 | 自分で切り替えを管理 | **右→5秒持ちかえ→左 を自動で案内** |
| 画面を離れたとき | （新規実装時）止まって見えた | **経過時刻から復帰** |

検証: 実データ40パターンすべて正しく解釈（異常値0）。4秒×左右2回の実走行で
右→休憩→左→完了まで到達を確認。コンソールエラー0

---

## 2026-08-10 — スコアの潰れ・煽り表現・不要な10MBダウンロードの是正

**スコア下限35の作り直し**（`analyzer.js`）
- 問題（実測）: 素点が50を切ると一律35点にクランプされ、
  **問題5個の人も14個の人も同じ35点**。14個→6個に改善しても**点数が1点も動かない**（前後比較が無意味）
- 対策: 50点以上は**完全に据え置き**（既存ユーザーの点数を動かさない）、
  50点未満だけ 35〜50 に圧縮して順序を残す。不安を煽らないため35は下回らせない
- 実測: 全部盛り(14件) 35点 → 重4+中1(5件) 42点 と、**改善が+7点として見える**（変更前は差0）
- 50点以上の全整数について変化なしを検証済み

**煽り表現の撤去**（`analyzer.js`）
- `CRITICAL / 多面的な機能不全。専門家との併用を推奨します。` のような英字警告＋断定を廃止
- 点数が低い人ほどアプリを必要としているのに、そこで怖がらせて離脱させていた
- 変更後: とても良い／良い／伸びしろあり／じっくり整えたい／**変えがいがある**
  最下位帯も「整えたい点が多めです。無理のない範囲で大丈夫。痛みが続くときは専門家にもご相談ください。」

**AIモデル(約10MB)の先読み条件**（`app.js`）
- 問題: ページを開いた全員に無条件で先読みしていた。
  簡易診断だけの人／今日のメニューを見に来ただけの再訪者にも約10MBの通信が発生
- 対策: 初回訪問（保存プランなし＝写真診断に進む可能性が高い）だけ従来どおり先読みし、
  それ以外は**アップロード欄に触れた瞬間**（pointerdown/focus/ファイル選択）に開始
- 実測: 初回訪問 4リクエスト → 再訪 **0リクエスト**、アップロード欄に触れると 4リクエスト

### 変更前 → 変更後

| 指標 | 変更前 | 変更後 |
|---|---|---|
| 問題14件→6件の改善 | 35点 → 35点（**差0**） | 35点 → 42点（**+7**） |
| 50点以上のスコア | — | **完全据え置き**（全整数で検証） |
| 最下位帯の表示 | `CRITICAL` 多面的な機能不全 | **変えがいがある**／無理のない範囲で大丈夫 |
| 再訪時の通信 | 毎回 約10MB の先読み | **0**（写真を使う気配があるときだけ） |

---

## 2026-08-10 — 結果ページから行動までの距離を短縮

**問題（実測）**
- スマホ幅375pxで結果ページの高さは **17,628px＝21.7画面**
- 実際にやること（今日のメニュー）は **12,558px＝15.5画面下**
- しかも途中に「姿勢の豆知識」1,852pxが挟まっており、行動の前に読み物が来ていた

**対策**（`index.html` / `app.js` / `style.css`）
- 「姿勢の豆知識」を30日プログラムの**後ろ**へ移動（STEP番号も振り直し）
  - 新順序: 02 サマリー → 03 姿勢のポイント → 04 コース選び → 05 今日のメニュー → 06 30日プログラム → 07 豆知識
- 診断サマリーの直後に「次にやること」バーを追加
  「ここまでが診断結果です／この下に『なぜそうなるか』『コース選び』が続きます。先に今日の分をやってもOKです」
  ＋ **▶ 今日のメニューへ（DAY 1・4種・約10分）** のボタン1つで直行
- 完走後は文言が変わり「▶ 次の一歩を選ぶ」になる

**スクロールが動かない不具合の修正**（`app.js`）
- 長距離の `scrollIntoView({behavior:'smooth'})` が環境によって**無反応**になり、
  ボタンを押しても何も起きないように見えていた（実測: 押しても scrollY が 0 のまま）
- 共通ヘルパー `jumpTo()` を追加。即時ジャンプ＋フォント読込のズレを最大5回補正する方式に統一
- 置き換えた箇所: 結果へ / アップロードへ（4箇所）/ 今日のメニューへ（3箇所）の計8箇所
- 短距離のスクロール（エラー表示・簡易診断ボタン）は smooth のまま

### 変更前 → 変更後

| 指標 | 変更前 | 変更後 |
|---|---|---|
| 診断後に今日のメニューへ着く手段 | 15.5画面スクロール | **ボタン1タップ** |
| 行動より前にある読み物 | 豆知識 1,852px が中間に | **末尾へ移動** |
| 長距離スクロールの成否 | 環境により無反応（押しても動かない） | **8箇所すべて着地を確認**（着地位置 12px） |

健全性: 6セクションすべて正常描画（豆知識6件・コース5件・今日4種・30日10日分）、コンソールエラー0

---

## 2026-08-10 — 部位ごとの前後比較・未使用ファイルの削除

**部位ごとの Before/After**（`app.js` / `style.css`）
- 従来のマイデータの比較は、写真2枚＋総合点の差＋専門用語の指標表（「頭部前方位角(CVA)」等）だけだった
- 診断時に部位別スコアをセッションへ保存し、比較画面に **部位ごとの変化** を追加
  - 例: 💪 肩 53 → 79（+26）／🧍 背中・体幹 40 → 74（+34）
  - 「いちばん変わったのは 🧍 背中・体幹（+34点）でした」と一言でまとめる
  - 変化がないときは「姿勢は積み重ねで変わるので、続けることが効きます」と煽らずに伝える
- 専門用語の指標表は「くわしい数値で見る」に折りたたみ、初見の負担を下げた
- 旧形式のセッション（部位スコアなし）は「もう一度写真で診断すると次回から出ます」と明示

**`exercises.js` の削除**（815行）
- 参照ゼロを確認して削除（バックアップ: `~/.claude/backups/posture_2026-08-10/exercises.js.deleted`）
- 訂正: 当初「初回ロードが軽くなる」と説明したが誤り。**import されていないため元から配信されておらず**、
  配信量は変わらない。効果は死んだコードが消えることのみ
- あわせて初回ロードを実測: **gzip 約348KB**（別途 MediaPipe 約10MB）

---

## 2026-08-10 — Phase 4 完了（達成バッジ・最低実行ライン・30日後の導線）

**最低実行ライン**（`app.js`）
- 「⏱ 時間がない日はこれ1つだけ（種目名）」を追加。押すとその1種だけのガイドが開く
- 選ばれるのは**その日でいちばん問題直結な種目**（`program.targeted` から）
- 狙い: 忙しい日に「全部やるか、ゼロか」の二択を作らない

**達成バッジ**（`app.js` / `style.css`）
- 3日🌱／1週間🌸／2週間🌿／3週間🌼／30日完走👑。未達は🔒で先が見える
- 「あと◯日で 🌸 1週間」と次の目標を常に1行で提示

**30日完走後の導線**（`app.js` / `storage.js` / `style.css`）
- 従来: 「やり切りました」の1行で終わり（次にやることがない）
- 変更後: 実績サマリー（実施日数／最後までできた日数／楽に感じた日数）＋バッジ＋
  ①📷 いまの姿勢を撮って30日前と比べる ②📊 これまでの記録を見る ③▶ 次の30日をはじめる
- 写真診断が2回以上あれば「前回 ◯点 → 最新 ◯点」を表示
- **次の30日**: `startRound()` で done/logs をリセットしつつ、前回の要約を `history` に保存。
  前回の体感（楽 > きつい なら +1／きつい多なら −1）を次の基準として引き継ぐ

**調整の累積**（`app.js`）
- 問題: チェックポイントの調整が毎回**上書き**だったため、2周目の引き継ぎ基準や
  前回の判断が「現状維持」判定で黙って消えていた
- 対策: 差分として積み上げ、level は [−1, +1]、種目数は [−2, +1] でクランプ
- 実測: 2周目引き継ぎ+1 →（維持）+1 →（きつい）0 →（きつい）−1 と段階的に下がる

### 変更前 → 変更後

| 指標 | 変更前 | 変更後 |
|---|---|---|
| 忙しい日の選択肢 | 全部やる or できなかった | **＋「これ1つだけ」** |
| 節目の可視化 | なし | **5段階のバッジ＋次の目標** |
| 30日完走後 | 「やり切りました」の1行 | **実績サマリー＋3つの導線** |
| 31日目以降 | 行き場なし | **次の30日（前回の体感を引き継いだ基準で再生成）** |
| 途中評価の重ね方 | 毎回上書き（前回の判断が消える） | **累積＋クランプ** |

健全性: **5040通り**（5コース×3経験×3調整×4種目数×4調整幅×7痛み条件）で 枠不足0・連続日重複0・危険種目0。コンソールエラー0

---

## 2026-08-10 — Phase 4（伴走）実装

**TOP#6 実施記録の粒度up**（`storage.js` / `app.js` / `style.css`）
- 「✓ 完了」1ビット → **できた／一部できた／できなかった** の3択
- 記録直後に体感を1タップ（🙂楽だった／😌ちょうど／😣きつかった・スキップ可）
- `pl_progress_v1` に `logs[day] = {status, feel, date}` を追加（旧データの `done` だけでも読める）
- 「できなかった」は日を進めない（翌日また同じDAYから）。記録は残して次の調整に使う
- **🔥 連続日数**を今日のメニューに表示（`currentStreak()`・2日以上空いたら途切れ）

**TOP#7 途中評価と自動適応**（`app.js` / `program.js` / `index.html` / `style.css`）
- Day 7 / 14 / 21 を越えると、直近7日の記録から次の日程を自動調整するカードを表示
  - 「きつかった」が4割以上 → 強度をひとつやさしく
  - 「楽だった」が6割以上 → 少し歯ごたえのある内容に
  - できない日が4割以上／途中までが5割以上 → 1日の種目数を1段減らす
- 「今のままがいい」で調整を断れる（押しつけない）
- 調整内容は `pl_progress_v1.adapt` に保存し、以降の生成に反映

**調整を「その日から」効かせる**（`program.js`）
- 問題: 安全フィルタ通過後の強度は実質1〜2の2段階しかなく、Day8時点では
  「やさしくしました」と言っても**メニューが1つも変わらなかった**（実測で確認）
- 対策: `tune` を追加し、回数・秒数を全フェーズで ±15% 調整。やさしく側は元値の6割まで下げてよい
  （通常は元値を下回らせないガードがあり、それが効いていた）
- 実測: DAY8「各20秒→各15秒」「10回→9回」「8回→7回」と当日から変わる

**強度帯の作り直し**（`program.js`）
- 問題: 「楽だった→歯ごたえを」を選ぶと **Phase2の平均強度が 2.00→1.36 に下がっていた**
  （exp を上げると上限だけ広がり、軽い種目が入り込むため）
- 対策: 上限/下限の計算をやめ、`INTENSITY_BAND[exp][phase] = [下限, 上限]` の明示テーブルに変更

**表示の実態合わせ**（`app.js` / `index.html`）
- 「セルフケア2種＋トレーニング2種（約10〜15分）」固定 → 実際の種目数と所要時間（5分/10分/15分/20分）

### 変更前 → 変更後（実測）

| 指標 | 変更前 | 変更後 |
|---|---|---|
| 1日の記録 | 完了の1ビット | **3択＋体感（4段階×3）** |
| 30日の途中調整 | なし（Day1の計画が30日固定） | **Day7/14/21に自動評価＋本人が承諾/拒否** |
| 「やさしくした」の実効果 | Day8時点で**変化なし** | **当日から回数・秒数が減る**（20秒→15秒 等） |
| exp別 Phase2平均強度（some→regular） | 2.00 → **1.36（逆行）** | 2.00 → 2.00、回数は 25.0 → **27.3** |
| Phase別の平均回数（exp=some） | — | P1 12.4 → P2 25.0 → **P3 30.1** |
| 継続の可視化 | 「n/30日 完了」のみ | **＋🔥 連続日数** |
| 所要時間の表示 | 常に「約10〜15分」 | **選んだ時間どおり**（2種=約5分／6種=約20分） |

健全性: **1260通り**（5コース×3経験×3調整×4種目数×7痛み条件）で 枠不足0・連続日重複0・危険種目0。段階的負荷は45通りすべて逆行なし。コンソールエラー0

---

## 2026-08-10 — Phase 2 後半（部位別スコア・4プラン差別化）＋ Phase 3 一部

**TOP#9 部位別サブスコア**（`analyzer.js` / `app.js` / `index.html` / `style.css`）
- `calcRegionScores()` を追加。5領域（頭・首／肩／背中・体幹／骨盤・腰／脚・足）に分解
- 各領域をタップすると【📷 見たところ（実測値と目安）】【📉 この点になった理由】【📈 上げるには】が開く
- 写真がなく、お悩みにも出てこない領域は **100点にせず「—（未評価）」** にした
  （見てもいない部位に満点を出すのは誤解を招くため）

**TOP#10 4プランの差別化**（`prescription-matrix.js`）
- 原因を特定: `sn_`（高齢者向け安全種目175種）が**全4コースにタグ付けされており**、
  どのコースを選んでも同じ89種がプールの67〜73%を占めていた
- `courseAffinity()` を追加。①専用DB由来＝そのコースの中核 ②`sn_`は動きの性格でコースへ振り分け
  （整体=ゆるめる/動かす・自重トレ=鍛える/支える・ヨガ=のばす/呼吸・ピラティス=体幹/背骨）
- `applyCourseCharacter()` で中核から順に採用。**問題直結(targeted)は最低8件を確保**してから絞る
  （絞りすぎると毎日同じ1種目になるため）

**Phase 3 意図的反復の明示**（`program.js` / `app.js` / `style.css`）
- 30日で3回以上出てくる種目に「🔁 30日で◯回入っています（あなたの姿勢に直接効く基本の種目なので、くり返して身につけます）」
- `build30DayProgram()` の戻り値に `targeted` を添付し、「あなた向け」か「土台」かを言い分ける

**表示の統一**（`app.js`）
- 30日カレンダーの種目名が正式名（例「チンタック」）のままだったのを、
  他画面と同じ平易名（例「あご引きエクササイズ」）に統一

**安全側の補強**（`program.js` / `app.js`）
- 痛み配慮フラグ・主訴フォーカスを `build30DayProgram(..., {pain, focus})` で**明示的に渡す**方式に変更。
  モジュール共有状態への依存をやめ、設定漏れで禁忌フィルタが無効化されるのを防ぐ
- 検証中に判明: `import './x.js?v=6'` のようにクエリを付けると**モジュール実体が二重化**し、
  `setPainAvoidance()` が別インスタンスに向いて膝痛ユーザーに危険種目が12件通過した。
  この変更は取り消し、`style.css?v=N` と `app.js?v=N` のみに留めた

### 変更前 → 変更後（実測）

| 指標 | 変更前 | 変更後 |
|---|---|---|
| コース間の30日重複（ヨガ×ピラティス） | 共通 70 / 111・103 | **共通 4 / 60・59** |
| コース間の30日重複（整体×ピラティス） | 共通 67 / 112・103 | **共通 5 / 65・59** |
| コース間の30日重複（整体×ヨガ） | 共通 82 / 112・111 | **共通 26 / 65・60** |
| 30日で使うtechnique上位（ピラティス） | strength:26 mobility:32 | **pilates:59** mobility:37 core:12 |
| Day1の顔ぶれ | 4コース中3コースがほぼ同一 | **4コースとも別内容** |
| 姿勢スコアの粒度 | 総合1点のみ | **総合＋5部位（根拠・理由・上げ方つき）** |
| 30日の変化の幅（コース選択時） | 112種 | 59〜65種（各種目 最大3〜7回・**理由を明示**） |
| 同じ種目の最多出現（ヨガ・単一問題） | 12回（説明なし） | **7回（説明あり）** |

健全性（回帰なし）: 連続日重複0 / 枠不足0 / 禁忌6条件×4コースで危険種目0 / 段階的負荷は全8パターン上昇 / コンソールエラー0

---

## 2026-08-09 — Phase 1（信頼の土台）＋ Phase 2 前半 実装

### Phase 1: 信頼の土台

**TOP#2 医療線引きの再構成**（`app.js` / `style.css`）
- 削除: 「🔴 短縮/過緊張・🟡 弱化/機能低下」の筋名断定リスト（写真では確認できないため）
- 追加: 3層表示 `evidenceBlocks()`
  - 📷 写真から確認できたこと（計測値の提示。症状ベースの場合は「写真ではなく申告から」と明示）
  - 🎯 考えられる改善の方向（「一般的に〜と言われています」と推測であることを明示）
  - ❓ 写真だけでは分からないこと（筋の硬さ・筋力・可動域・痛みの原因／受診案内）

**TOP#3 写真品質チェック＋撮影ガイド**（`app.js` / `index.html` / `style.css`）
- `checkPhotoQuality()` 追加。追加ライブラリ0で以下を判定:
  - 主要9点の平均 visibility < 0.55 → 「人物がはっきり写っていない」
  - 足首の visibility < 0.4 / 下端はみ出し → 「足元が写っていない」
  - 頭部の枠外・縦の占有 < 0.45・左右はみ出し → 構図の警告
  - canvas 64px 縮小の平均輝度 < 55 / > 235 → 暗い・明るすぎ
  - 検出人数 > 1 → 「複数の人が写っている」
- 結果画面に警告バナー＋「写真を撮り直す」導線（弾かずに信頼度を伝える方式）
- 撮影ガイド（折りたたみ）: 守る5点／NG5例

**TOP#8 安全導線**（`app.js` / `index.html` / `prescription-matrix.js` / `style.css`）
- 結果画面の先頭に免責1行（従来はフッターのみ）
- 各種目に「⚠ 痛み・しびれ・めまいが出た（中止する）」→ 中止ガイド（いますること3点／受診の目安4点／今日はお休みにする）
- 禁忌を **膝・腰の2部位 → 6条件**に拡張（首・肩・妊娠中・高血圧を追加）。首/肩/妊娠中/高血圧は手順文まで走査

### Phase 2 前半: 個別性

**TOP#4 初回プロフィール4問**（`index.html` / `app.js` / `storage.js` / `style.css`）
- 1タップで答えられる4問: とれる時間(5/10/15/20分)・運動経験(3択)・目的(2択)・年代(4択)
- `pl_prefs_v1` に保存し次回以降は選択済みで表示。セッションにも保存し復元時に再現
- 目的「見た目を整えたい」は重点部位に core/glutes/back を加算

**TOP#5 時間別の量＋TOP#1 段階的負荷**（`program.js`）
- `build30DayProgram(keys, course, {menuSize, exp})` に拡張
- 量: 5分=2種 / 10分=4種 / 15分=5種 / 20分=6種（休息日はセルフケアのみ）
- 強度帯: フェーズごとに上限＋**下限**を設定（下限が無いと軽い種目が終盤に流れ込み逆行する）
- 回数・秒数: `scaleDuration()` で Phase2 ×1.15 / Phase3 ×1.3（初心者は0.6倍緩和・経験者は1.3倍加速）。秒は5秒刻み、「分」は据え置き。DBは書き換えず表示用コピーのみ

### 変更前 → 変更後（実測）

| 指標 | 変更前 | 変更後 |
|---|---|---|
| 30日の負荷進行 | Phase1 1.47 → Phase3 **1.33（逆行）** | 全9パターンで**上昇**（+0.32〜+1.00）例: 1.00→1.74→**2.00** |
| 1日の量 | 4種固定 | 5分2種／10分4種／15分5種／20分6種 |
| 禁忌の条件数 | 2（膝・腰） | **6**（＋首・肩・妊娠中・高血圧）※全条件で枠不足0 |
| 写真品質チェック | 人物検出のみ | **6種類の自動判定＋撮り直し導線** |
| 診断結果の筋名断定 | あり | **削除**し3層（確認できたこと／推測／分からないこと）に再構成 |
| 実施中の異常時導線 | なし | **中止ガイド＋受診の目安** |
| 結果画面の免責 | なし（フッターのみ） | **結果先頭に表示** |
| 収集する本人情報 | 症状17＋自由記述 | **＋時間・運動経験・目的・年代** |

健全性（回帰なし）: 30日ユニーク93種 / 連続日重複0 / 枠不足0 / 危険種目0 / コンソールエラー0

---

## 2026-08-09 — STEP0〜38 監査（コード変更なし）

- プロジェクト全体を精読・実測し、`POSTURE_AI_AUDIT.md` / `POSTURE_AI_ROADMAP.md` を新規作成
- **総合評価: 50/100点**
- 主な実測値（改善の基準線）:

| 指標 | 現状値 |
|---|---|
| 30日の負荷進行 | Phase1 平均強度 1.47 → Phase3 1.33（**逆行**） |
| 初心者 vs 経験者の30日 | 30/30日 完全同一 |
| 5分希望 vs 20分希望の30日 | 30/30日 完全同一 |
| 姿勢分析の計測指標 | 側面5・正面4 |
| 写真品質チェック | 人物検出のみ（visibilityは未活用） |
| 中止基準を持つ種目 | 416/562（74%） |
| easyOption 欠損 | 146/562（26%） |
| beginnerSafe 付与 | 95/562（うち除外86種） |
| 妊娠中/高血圧の注記 | 1件 / 4件 |
| 初回ロード | gzip 281KB（+ MediaPipe 約10MB） |
| 免責の表示位置 | フッターのみ |

- 記録: LLM/生成AIは不使用（MediaPipe＋決定論的ロジックのみ）。課金発生箇所ゼロ

---

## これ以前の主な改善（本プロジェクト開始前・本番反映済み）

| 日付 | commit | 内容 |
|---|---|---|
| 2026-08-09 | `a695644` | 主訴フォーカス（同じ問題でも悩みで提案を変える）。腰痛 vs 下腹ぽっこり 30/30日一致 → 0/30日 |
| 2026-08-09 | `1ea86c8` | 痛み配慮レイヤー（膝・腰の禁忌フィルタ＋注意帯）。膝痛ユーザーの膝荷重系 15種 → 0種 |
| 2026-08-09 | `642ec3b` | 診断の優先順位を処方へ伝搬（重症度・悩みの票数・主訴） |
| 2026-08-09 | `df95a22` | ガイド付き実行モード「順番に始める」 |
| 2026-08-08 | `cfa11ea` | 再訪を1タップ・0スクロールに（おかえりカード） |
| 2026-08-08 | `9fae463` | 30日プログラムの進捗機能（DAY連動・完了チェック・進捗バー） |
| 2026-08-07 | `e4add26` | 写真なし簡易診断＋解析の待ち/失敗対策 |
| 2026-07-18 | `cac2958` | エクササイズ図を全幅・大きく表示 |
| 2026-07-18 | `71b9e30` | 全イラストを2コマ＋矢印の指導図に刷新（AI画像を全廃） |
| 2026-07-18 | `998ea4c` | 全処方363種を初心者テキスト基準に整備＋薬機/景表リスク表現91件を撤去 |
